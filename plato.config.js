// Plato configuration example
// Plato does not require a config file by default, but you can use CLI options.
// To analyze your src and tests, run:
// npx plato -r -d report src tests
// This will generate a 'report' folder with the analysis results.

// If you want to automate this, add to your package.json scripts:
// "scripts": {
//   ...existing scripts,
//   "plato": "plato -r -d report src tests"
// }
